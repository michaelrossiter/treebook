<!DOCTYPE html>
<html>
<head>
  <title>Twi-Test</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css">
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

	
<nav class="navbar navbar-inverse navbar-static-top">
  <div class="container">
  	<button class="navbar-toggle" data-toggle = "collapse" data-target = ".navbar-collapse">
  			<span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
	</button>
   	 <a href="#" class="navbar-brand" style="padding-top: 42px">Twi-Test</a>
     <div class="collapse navbar-collapse">
           <ul class="nav navbar-nav pull-left">
    	<li><a href="#"><%= link_to "All Statuses", statuses_path %></a></li>
      </ul>
      <ul class="nav navbar-nav pull-right">
    	 <% if user_signed_in? %>
        <li><a href="#"><%= link_to current_user.full_name, edit_user_registration_path %></a></li>
        <% else %>
        <li><a href="#"><%= link_to "Register", register_path %></a></li>
     <% end %>
     <% if user_signed_in? %>
        <li><a href="#"><%= link_to "Log Out", destroy_session_path(:user), :method => :delete %></a></li>
        <% else %>
        <li><a href="#"><%= link_to "Log In", login_path %></a></li>
      <% end %>
    </ul>

  </div>
  </div>
</nav>

<div class="container">
  <% flash.each do |type, message|%>
  <div class="alert <%= flash_class type %>">
    <button class="close" data-dismiss="alert">x</button>
    <%= message %></div>
  <% end %>
	<%= yield %>

</div>

</body>
</html>